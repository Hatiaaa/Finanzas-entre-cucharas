# Config de pnpm (formato ini clave=valor, NO JSON).
# No abortar `pnpm <script>` por el chequeo de dependencias previo
# (rompía el arranque del dev server). La aprobación de builds confiables
# (esbuild) vive en pnpm-workspace.yaml → allowBuilds.
verify-deps-before-run=false
